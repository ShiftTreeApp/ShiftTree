import { Typography } from "@mui/material";

interface EditShiftsTabProps {
  scheduleId: string;
}

export default function EditShiftsTab(_props: EditShiftsTabProps) {
  return (
    <>
      <Typography variant="h6">Edit shifts</Typography>
    </>
  );
}
