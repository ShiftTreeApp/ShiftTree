What are we storing:

Users:
    id, jsondata
        -gmail, password_hash, Username, online_status
SessionInfo:
    id, foreign_user_key, json-page_location(if they refresh the page?)

Organizations:
    -id, owner_id, json_Data()
                    -name, anything else?
OrganizatonMember
    -organiation foreign key, user_id_foreign key

ShiftTree:
    -id, organization